# Top-level dispatcher for the SPIRV-focused CI on amd-staging.
# Dispatches to per-platform reusable workflows. Add Windows by adding
# a windows_release job + spirv-ci-windows.yml.

name: SPIRV Compiler CI

on:
  pull_request:
    branches: [amd-staging]
    types: [opened, synchronize, reopened, labeled]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.event.number || github.sha }}
  cancel-in-progress: true

jobs:
  linux_release:
    name: Linux::release
    uses: ./.github/workflows/spirv-ci-linux.yml
    secrets: inherit

  windows_release:
    name: Windows::release
    uses: ./.github/workflows/spirv-ci-windows.yml
    secrets: inherit
